services:
  - type: web
    name: ai-bride-face-challenge
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: IMAGE_MODEL
        value: gpt-image-2
      - key: OPENAI_API_KEY
        sync: false
